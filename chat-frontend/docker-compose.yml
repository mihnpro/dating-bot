version: "3.9"

services:
  chat-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chat-frontend
    ports:
      - "3001:80"
    networks:
      - dating-bot-net
    restart: unless-stopped

networks:
  dating-bot-net:
    external: true
