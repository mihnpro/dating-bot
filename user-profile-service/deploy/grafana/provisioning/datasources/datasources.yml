apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    orgId: 1
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"

  - name: Loki
    uid: loki
    type: loki
    orgId: 1
    access: proxy
    url: http://loki:3100
    editable: true
    jsonData:
      maxLines: 1000
